fastapi==0.109.2
uvicorn[standard]==0.27.1 # Use [standard] for common extras like websockets, http-tools

# Pydantic (Core and Settings)
pydantic==2.6.1
pydantic-settings==2.1.0

# Supabase Client
supabase==2.4.1 # Or a more recent compatible version (check supabase-py releases)

# Langchain and OpenAI
langchain==0.1.12
langchain-openai==0.0.5
openai==1.12.0

# Utilities
python-dotenv==1.0.1
email-validator==2.1.0.post1 # Required by FastAPI for EmailStr
python-multipart==0.0.6 # For form data parsing if needed

