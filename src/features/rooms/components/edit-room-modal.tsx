// "use client";

// import { ResponsiveModal } from "@/components/responsive-modal";
// import { EditRoomForm } from "./edit-room-form";
// import { useEditRoomModal } from "../hooks/use-update-room-modal";

// export const EditRoomModal = () => {
//   const { roomId, close } = useEditRoomModal();

//   return (
//     <ResponsiveModal open={!!roomId} onOpenChange={close}>
//       {roomId && (<EditRoomForm onCancel={close} id={roomId} /> )}
//     </ResponsiveModal>
//   );
// };
